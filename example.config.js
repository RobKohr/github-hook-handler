module.exports ={
    postPath: 'someMadeupRandomString', 
    'repo-name-on-github:main': {
	cmd:'cd /project-directory; git pull; npm install; npm run build',
	notes:'Install and build the newest update'
    },
    port: 3187
}
